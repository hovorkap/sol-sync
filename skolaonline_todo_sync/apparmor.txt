# AppArmor profile for SkolaOnline ToDo Sync
# Allows outbound HTTPS connections only
#include <tunables/global>

profile skolaonline_todo_sync flags=(attach_disconnected,mediate_deleted) {
  #include <abstractions/base>

  # Restrict file access
  network inet tcp,
  network inet6 tcp,

  /usr/bin/python3* ix,
  /usr/local/bin/python3* ix,
  /app/** r,
  /data/** rw,
  /tmp/** rw,

  # Python libraries
  /usr/lib/python3*/** r,
  /usr/local/lib/python3*/** r,
}
